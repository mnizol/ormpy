#/bin/bash

# Set-up environment
export ORMPY_TEST_DATA_DIR=./test/data/

# Run tests
if [ "$1" -eq --cover ]; then
    nosetests --cover-inclusive --with-coverage --cover-erase --cover-package=lib
else
    nosetests --verbose
fi
